# File: requirements.txt

# --- Core Libraries ---
pandas          # Untuk pengolahan data (DataFrame) [cite: 365]
numpy           # Untuk operasi numerik, terutama pada hasil prediksi model [cite: 365]
streamlit       # Untuk membangun aplikasi web interaktif 

# --- NLP and Deep Learning Libraries ---
torch           # PyTorch, framework yang digunakan oleh IndoBERT/Transformers.
transformers    # Pustaka utama untuk model IndoBERT dan tokenizer-nya [cite: 364]

# --- Visualization Libraries ---
matplotlib      # Untuk visualisasi data (jika digunakan) [cite: 366]
seaborn         # Untuk visualisasi data statistik (jika digunakan) [cite: 366]
plotly          # Digunakan dalam contoh kode Streamlit untuk chart interaktif

# --- Optional/Utility Libraries (Jika digunakan dalam implementasi nyata) ---
# Jika Anda benar-benar melakukan scraping data saat runtime:
google-play-scraper 

safetensors
# Jika Anda menggunakan library untuk Normalisasi yang lebih mendalam:
# sastrawi
